# 仓库协作约定

使用中文作为主要语言对话和提交。

默认不新增 hash、不冻结 contract、不建 baseline、不加 gate。除非能明确说出一个具体失败场景，并且讲清楚为什么 Git、版本号、主键、事务、唯一约束、类型、普通测试全都挡不住它时才允许加。
不移除已有的安全措施；认证、数据安全、不可逆操作、正式发布等高风险环节，仍按项目要求处理。

## CodeGraph

当仓库根目录存在 `.codegraph/` 时，理解或定位代码前优先使用 `codegraph explore "问题或符号"` 与 `codegraph node <符号或文件>`；也可使用对应 MCP 工具。无索引时跳过，不自动创建索引。修改代码后可运行 `codegraph sync`。

## 验证

参考 CONTRIBUTING.md。默认测试不得连接真实 SMB、写入真实钥匙串或替换用户配置。开源文档与测试样例中使用脱敏路径。
